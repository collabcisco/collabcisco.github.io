import { default as Logger, ILogger } from 'js-logger';
interface ExtendedLogger extends ILogger {
    errorAndThrow(message: string): never;
}
declare const logger: ExtendedLogger;
export { Logger, logger };
